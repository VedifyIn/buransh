/**
 * Re-exports from canonical locations in src/.
 */
export { stripEmpty, truncateStrings, optimizeGraph } from '../src/utils/optimizer';
export type { OptimizeResult } from '../src/utils/optimizer';
